# Police agent secret placeholders. Copy to .env (gitignored) and fill locally. NEVER commit .env.
# Day-1 needs NO secrets (stdlib-only, offline). Later phases may add:
# ANTHROPIC_API_KEY=        # only if opt-in claude_api banter is used (default is offline template)
