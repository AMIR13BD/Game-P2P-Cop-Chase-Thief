"""Deterministic baseline and reference opponents for evaluation (P12).

These are intentionally simpler than the championship portfolio; they exist to
benchmark it. The `registry` splits them into a tuning set and a held-out set."""
