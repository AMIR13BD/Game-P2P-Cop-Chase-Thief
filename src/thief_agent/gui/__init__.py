"""Local, offline visualization (P20 replay viewer + P21 GUI).

All rendering is text-based and headless-testable. The live GUI shows only the local
player's truth and can never reveal the opponent's position; the replay viewer works
on post-audit records where both trajectories are legally revealed."""
